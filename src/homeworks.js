/**
 *
 * @param {string} str
 * @param {string} search
 * @returns {number}
 */
function indexOfString(str, search) {
	// your code
}

/**
 *
 * @param {string} str
 * @param {string} search
 * @returns {number}
 */
function lastIndexOfString(str, search) {
	// your code
}

/**
 *
 * @param {string} str
 * @param {number} startIdx
 * @param {number} endIdx
 * @returns {string}
 */
function sliceString(str, startIdx, endIdx) {
	// your code
}

/**
 *
 * @param {string} str
 * @param {string} search
 * @returns {boolean}
 */
function startsWithString(str, search) {
	// your code
}

/**
 * @param {string} str
 * @param {string} search
 * @returns {boolean}
 */
function endsWithString(str, search) {
	// your code
}

/**
 *
 * @param {string} str
 * @param {number} startIdx
 * @param {number} count
 * @returns {string}
 */
function subStr(str, startIdx, count) {
	// your code
}

console.log(indexOfString('PDP BEST UNIVERSITY', 'BEST')); // 4
console.log(lastIndexOfString('PDP BEST UNIVERSITY', 'E')); // 13
console.log(sliceString('PDP BEST UNIVERSITY', 4, 13)); // BEST UNIVERSITY
console.log(startsWithString('PDP BEST UNIVERSITY', 'PDP')); // true
console.log(endsWithString('PDP BEST UNIVERSITY', 'UNIVERSITY')); // true
console.log(subStr('PDP BEST UNIVERSITY', 4, 4)); // BEST
